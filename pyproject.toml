[project]
name = "heathcliff-bot"
version = "0.1.0"
description = "Posts a daily heathcliff thread to a Zulip channel."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "requests>=2.33.1",
    "zulip>=0.9.1",
]
